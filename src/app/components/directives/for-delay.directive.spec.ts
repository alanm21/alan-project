import { ForDelayDirective } from './for-delay.directive';

describe('ForDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new ForDelayDirective();
    expect(directive).toBeTruthy();
  });
});
